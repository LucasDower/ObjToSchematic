name: Tests-Sandbox
on: push
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: Install modules
      working-directory: ./Sandbox
      run: npm ci
    - name: Run tests
      working-directory: ./Sandbox
      run: npm test
